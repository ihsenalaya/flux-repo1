apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: external-secrets
  namespace: external-secrets
spec:
  interval: 10m
  chart:
    spec:
      chart: external-secrets
      version: ">=0.9.0"
      sourceRef:
        kind: HelmRepository
        name: external-secrets
        namespace: flux-system

  # 👉 GARANTIT l’installation & mise à jour des CRDs depuis le chart
  install:
    crds: Create         # <— IMPORTANT
    timeout: 10m
    remediation: { retries: 3 }
  upgrade:
    crds: CreateReplace  # <— IMPORTANT
    timeout: 10m
    remediation: { retries: 3 }

  values:
    # 👉 Installe aussi les CRDs via les valeurs du chart (double sécurité)
    installCRDs: true

    # Le ServiceAccount est géré en overlay (Workload Identity)
    serviceAccount:
      create: false
      name: external-secrets
