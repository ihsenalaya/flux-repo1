apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: external-secrets
  namespace: external-secrets
spec:
  values:
    serviceAccount:
      annotations:
        azure.workload.identity/client-id: "<ESO_APP_CLIENT_ID>"  # App reg (federated) pour ESO controller
